import { GetDayReferencePipe } from './get-day-reference.pipe';

describe('GetDayReferencePipe', () => {
  it('create an instance', () => {
    const pipe = new GetDayReferencePipe();
    expect(pipe).toBeTruthy();
  });
});
